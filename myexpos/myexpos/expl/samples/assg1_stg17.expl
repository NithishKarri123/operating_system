int main()
{
  decl
  str name;
  int val,a;
  enddecl
  
  begin
  a=10;
  val =exposcall("Write",-2,"Nitish"); 
   val=exposcall("Read",-1,name);
   val=exposcall("Exec",name);
   if(val==-1) then
   	val=exposcall("Write",-2,a);
   	endif;
   return 0;
  end
}
